/** Synthetic id for the virtual "Unfiled" folder (notes with no folder). */
export const UNFILED_FOLDER_ID = '__all-notes__'

/** Synthetic id for the virtual "All Notes" folder (every note, regardless of folder). */
export const ALL_NOTES_FOLDER_ID = '__every-note__'

/** Synthetic id for the virtual "Shared with me" folder. */
export const SHARED_WITH_ME_FOLDER_ID = '__shared-with-me__'
